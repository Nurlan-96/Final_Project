@using FinalProject.Domain.Entities
@model IEnumerable<CategoryEntity>

<div class="container-fluid">
    <div class="row">
        <!-- Sidebar -->
        <nav class="col-md-3 col-lg-2 d-md-block bg-light sidebar">
            <div class="position-sticky">
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link active" asp-controller="dash" asp-action="index" data-bs-toggle="tab">
                            <i class="fa fa-briefcase me-2"></i> JobPosts
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="company" asp-action="index" data-bs-toggle="tab">
                            <i class="fa fa-building me-2"></i> Companies
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="category" asp-action="index" data-bs-toggle="tab">
                            <i class="fa fa-list-alt me-2"></i> Categories
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="report" asp-action="index" data-bs-toggle="tab">
                            <i class="fa fa-chart-bar me-2"></i> Reports
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="user" asp-action="index" data-bs-toggle="tab">
                            <i class="fa fa-users me-2"></i> Users
                        </a>
                    </li>
                </ul>
            </div>
        </nav>

        <!-- Main content -->
        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
            <div class="tab-content">
                <!-- JobPosts -->
                <div id="jobposts" class="tab-pane fade show active p-4">
                    <h2>Job Posts</h2>
                    <button class="btn btn-primary mb-3">Add New Job Post</button>
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Name</th>
                                <th>Created Date</th>
                                <th>Last Updated</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var category in @Model)
                            {
                                <tr>
                                    <td>@category.Id</td>
                                    <td>@category.Name</td>
                                    <td>@category.CreatedDate</td>
                                    <td>@category.UpdatedDate</td>
                                    <td>
                                        <button class="btn btn-sm btn-warning">Edit</button>
                                        <button class="btn btn-sm btn-danger">Delete</button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>
</div>
